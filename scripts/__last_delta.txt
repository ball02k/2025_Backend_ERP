FRONTEND_DELTA_PROMPT
--------------------------------
You are syncing the FRONTEND after backend API changes.

API CHANGES:
- CHANGED GET /api/suppliers?approved=true&capability=tag1,tag2 → now filters suppliers that match ANY provided capability tags and returns { data: [{ id, name, capabilityTags, insuranceValid, accreditations }] }

UPDATE THESE CALLS/COMPONENTS:
- SupplierFilter.jsx: adjust fetch logic and response handling for new shape

TEST UPDATES:
- Update supplier capability filter tests to expect { data } response and ANY-tag logic

After applying, bump localStorage.lastSeenCatalogHash to "<CURRENT_HASH>".
--------------------------------
/END FRONTEND_DELTA_PROMPT
